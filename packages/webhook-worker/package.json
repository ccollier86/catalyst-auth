{
  "name": "@catalyst-auth/webhook-worker",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "lint": "echo 'No lint configured for @catalyst-auth/webhook-worker'",
    "test": "vitest run"
  },
  "dependencies": {
    "@catalyst-auth/contracts": "^0.1.0",
    "@catalyst-auth/telemetry": "^0.1.0"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
